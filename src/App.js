import { useState } from "react";

function App() {
  const [number, setNumber] = useState(0);

  if (number > 5) {
    const [text, setText] = useState("");
  }

  return <></>;
}

export default App;
